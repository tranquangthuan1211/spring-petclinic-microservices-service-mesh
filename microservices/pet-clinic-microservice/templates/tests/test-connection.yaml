apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pet-clinic-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "pet-clinic-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pet-clinic-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
