apiVersion: v1
kind: Service
metadata:
  name: {{ include "pet-clinic-microservice.fullname" . }}
  labels:
    {{- include "pet-clinic-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pet-clinic-microservice.selectorLabels" . | nindent 4 }}
